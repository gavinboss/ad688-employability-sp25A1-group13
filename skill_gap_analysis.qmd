```{python}
import pandas as pd

# Example skills data (can be updated later with real values)
skills_data = {
    "Name": ["Anu", "Cindy", "Gavin"],
    "Python": [5, 3, 4],
    "SQL": [4, 2, 5],
    "Machine Learning": [3, 1, 4],
    "Cloud Computing": [2, 2, 3]
}

df_skills = pd.DataFrame(skills_data).set_index("Name")
df_skills
```